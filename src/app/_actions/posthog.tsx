// const handleClick = () => {
//   "use server";
//   const cookieStore = cookies();
//   const sessionId = cookieStore.get("sessionId"); // Retrieve sessionId from cookies

//   posthog.capture("whatsapp", { sessionId }); // Send the event with the sessionId
// };
